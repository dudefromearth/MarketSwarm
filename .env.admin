# admin_console only (safe to keep locally; do NOT commit secrets)
# Canonical service names
BOOTSTRAP_REDIS_URL=redis://system-redis:6379
MARKET_REDIS_URL=redis://market-redis:6380
TRUTH_REDIS_KEY=truth:doc

# Fallback to localhost if system-redis is not resolvable
# (admin script will detect and auto-rewrite these internally)
LOCAL_SYSTEM_REDIS=redis://127.0.0.1:6379
LOCAL_MARKET_REDIS=redis://127.0.0.1:6380

# Truth paths (autodetected if missing)
TRUTH_FILE=root/truth.json
TRUTH_SECRETS_FILE=root/truth_secrets.json